# 🚂 Деплой на Railway - Пошаговая инструкция

Это подробное руководство по развертыванию проекта Tabs TG на Railway.

## Предварительные требования

- ✅ Аккаунт на [Railway](https://railway.app)
- ✅ GitHub репозиторий с вашим проектом
- ✅ Telegram Bot Token от [@BotFather](https://t.me/botfather)

## 📝 Шаг 1: Подготовка репозитория

1. **Убедитесь что все изменения закоммичены**
```bash
git add .
git commit -m "Prepare for Railway deployment"
git push origin main
```

2. **Проверьте что `.env` файл в `.gitignore`**
   - ⚠️ **ВАЖНО**: Никогда не коммитьте `.env` с секретами!
   - Используйте `.env.example` как шаблон

## 🎯 Шаг 2: Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Авторизуйте Railway для доступа к GitHub
5. Выберите репозиторий `tabs-tg`

## 🗄️ Шаг 3: Добавление PostgreSQL

1. В вашем проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Переменная `DATABASE_URL` будет доступна автоматически

## 🔧 Шаг 4: Настройка Backend API Service

1. **Создайте новый сервис**
   - Нажмите **"+ New"** → **"GitHub Repo"** → выберите ваш репозиторий

2. **Настройте сервис**
   - **Name**: `api`
   - **Root Directory**: `/backend`
   - **Build Command**: (оставьте пустым, используется Dockerfile)
   - **Start Command**: `uvicorn app.main_api:app --host 0.0.0.0 --port $PORT`

3. **Добавьте переменные окружения**
   - Перейдите в **Settings** → **Variables**
   - Railway автоматически добавит `DATABASE_URL` если вы подключили Postgres
   - Добавьте вручную:
     - `PORT`: `8000` (или оставьте пустым, Railway установит автоматически)

4. **Подключите к PostgreSQL**
   - В разделе **Settings** → **Service**
   - Нажмите **"Connect"** и выберите ваш PostgreSQL сервис

5. **Включите публичный домен**
   - **Settings** → **Networking**
   - Нажмите **"Generate Domain"**
   - Скопируйте URL (например: `https://api-production-xxxx.up.railway.app`)

## 🤖 Шаг 5: Настройка Bot Service

1. **Создайте еще один сервис из того же репозитория**
   - **Name**: `bot`
   - **Root Directory**: `/backend`
   - **Start Command**: `python -m app.main_bot`

2. **Добавьте переменные окружения**
   - `DATABASE_URL`: (автоматически от PostgreSQL)
   - `BOT_TOKEN`: ваш токен от @BotFather

3. **Подключите к PostgreSQL** (как в шаге 4.4)

4. **НЕ генерируйте публичный домен** для bot сервиса (он не нужен)

## 🎨 Шаг 6: Настройка Frontend Service

1. **Создайте третий сервис**
   - **Name**: `frontend`
   - **Root Directory**: `/frontend`
   - **Build Command**: (оставьте пустым, используется Dockerfile)
   - **Start Command**: (оставьте пустым, используется CMD из Dockerfile)

2. **Добавьте переменные окружения**
   - `NEXT_PUBLIC_API_URL`: URL вашего API сервиса (из шага 4.5)
     - Пример: `https://api-production-xxxx.up.railway.app`

3. **Включите публичный домен**
   - **Settings** → **Networking**
   - Нажмите **"Generate Domain"**
   - Это будет URL вашего приложения!

## ✅ Шаг 7: Проверка деплоя

1. **Проверьте логи каждого сервиса**
   - API должен показать: `Application startup complete`
   - Bot должен показать: `Bot started` или подобное
   - Frontend должен успешно собраться

2. **Проверьте health check API**
   - Откройте `https://your-api-url.railway.app/health`
   - Должен вернуть: `{"status": "healthy", "service": "tabs-tg-api"}`

3. **Откройте frontend**
   - Перейдите на URL вашего frontend сервиса
   - Попробуйте авторизоваться через Telegram

## 🔄 Шаг 8: Настройка автоматического деплоя

Railway автоматически настроит webhook для вашего репозитория:
- При каждом push в `main` ветку проект будет автоматически деплоиться
- Вы можете отключить это в **Settings** → **Service** → **Deploy Triggers**

## 🎯 Итоговая структура проекта

```
Railway Project
├── PostgreSQL Database
│   └── DATABASE_URL (автоматически)
│
├── API Service (backend)
│   ├── Root: /backend
│   ├── Command: uvicorn app.main_api:app --host 0.0.0.0 --port $PORT
│   ├── Variables:
│   │   ├── DATABASE_URL (from Postgres)
│   │   └── PORT (from Railway)
│   └── Public Domain: ✅
│
├── Bot Service (backend)
│   ├── Root: /backend
│   ├── Command: python -m app.main_bot
│   ├── Variables:
│   │   ├── DATABASE_URL (from Postgres)
│   │   └── BOT_TOKEN (manual)
│   └── Public Domain: ❌
│
└── Frontend Service
    ├── Root: /frontend
    ├── Command: (from Dockerfile)
    ├── Variables:
    │   └── NEXT_PUBLIC_API_URL (manual)
    └── Public Domain: ✅
```

## 🐛 Troubleshooting

### API не стартует
- **Проверьте**: `DATABASE_URL` установлен и сервис подключен к Postgres
- **Проверьте логи**: может быть ошибка миграции БД
- **Решение**: Railway автоматически создаст таблицы при старте

### Bot не отвечает
- **Проверьте**: `BOT_TOKEN` правильный
- **Проверьте логи**: ошибки подключения к Telegram API
- **Примечание**: Бот использует polling, не webhook (работает из коробки)

### Frontend показывает ошибки API
- **Проверьте**: `NEXT_PUBLIC_API_URL` указывает на правильный URL API
- **Проверьте**: API сервис запущен и доступен
- **Проверьте**: CORS настроен правильно (уже настроено в коде)

### Ошибка "Failed to build"
- **Frontend**: Убедитесь что `output: 'standalone'` в `next.config.ts`
- **Backend**: Проверьте что все зависимости в `requirements.txt`

### База данных пустая после деплоя
- Railway автоматически создаст таблицы при первом запуске API
- Если нет - проверьте логи API на ошибки миграции

## 💡 Полезные советы

1. **Мониторинг**
   - Используйте Railway Metrics для отслеживания использования ресурсов
   - Настройте уведомления для критических ошибок

2. **Переменные окружения**
   - Используйте Railway UI для управления секретами
   - Никогда не коммитьте `.env` файлы

3. **Логи**
   - Railway хранит логи в реальном времени
   - Используйте фильтры для поиска ошибок

4. **Стоимость**
   - Railway предоставляет $5 бесплатно каждый месяц
   - Следите за использованием в Dashboard

5. **Обновления**
   - Просто делайте `git push` - Railway автоматически задеплоит
   - Можно откатиться к предыдущей версии в UI

## 🎉 Готово!

Ваше приложение теперь доступно онлайн! Поделитесь ссылкой на frontend с пользователями.

**Следующие шаги:**
- Настройте custom domain (опционально)
- Настройте мониторинг и алерты
- Добавьте CI/CD тесты перед деплоем
